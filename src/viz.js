import React from 'react'

const Viz = (props) => {
  return (
    <svg className='thumbnail' width={200} height={200}>
      <g>
        <rect x={20} y={140} width={100} height={180} fill={'#8181EE'} />
      </g>
    </svg>
  )
}

export default Viz
